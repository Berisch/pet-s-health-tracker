version: '3.8'

services:
  filya:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./server/data:/app/server/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
